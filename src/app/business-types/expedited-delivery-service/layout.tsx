import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Expedited Delivery Service Insurance",
};

export default function ExpeditedDeliveryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
