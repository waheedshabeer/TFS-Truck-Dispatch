import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Other Trucking Operations Insurance",
};

export default function OtherTruckingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
